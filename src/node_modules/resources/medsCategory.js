import Authenticated from "resources/Authenticated";

export default class MedsCategoryResource extends Authenticated {
  id = null;
  name = "";

  pk() {
    return this.id;
  }

  static urlRoot = `${process.env.REACT_APP_API_HOST}/api/medscategory/`;
}
