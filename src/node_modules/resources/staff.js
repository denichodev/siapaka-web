import Authenticated from "resources/Authenticated";

export default class StaffResource extends Authenticated {
  id = null;
  name = "";
  address = "";
  phoneNo = "";
  role = null;
  outlet = null;

  pk() {
    return this.id;
  }

  static urlRoot = `${process.env.REACT_APP_API_HOST}/api/users/`;
}
