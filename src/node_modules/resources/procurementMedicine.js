import Authenticated from "resources/Authenticated";

export default class ProcurementMedicineResource extends Authenticated {
  id = null;

  pk() {
    return this.id;
  }

  static urlRoot = `${process.env.REACT_APP_API_HOST}/api/procurementmedicines/`;
}
