import Authenticated from "resources/Authenticated";

export default class DoctorResource extends Authenticated {
  id = null;
  name = "";
  address = "";
  phoneNo = "";

  pk() {
    return this.id;
  }

  static urlRoot = `${process.env.REACT_APP_API_HOST}/api/doctors/`;
}
